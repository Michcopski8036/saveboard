// The accent strip that runs along the top of a card — saved links, blog posts
// and guides all use this one, so they read as the same product.
//
// Tone-on-tone by request: brand purple easing into indigo, nothing else. The
// earlier multi-stop version (blue head, coral tail) read as a rainbow against
// the white cards.
export const CARD_STRIP_GRADIENT =
  'linear-gradient(to right, #A259FF 0%, #6366F1 100%)';
