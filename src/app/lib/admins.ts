// Re-exported so app code keeps a short import path. The list itself lives in
// src/shared/admins.ts because the API functions import it too.
export { ADMIN_EMAILS, isAdmin } from '../../shared/admins';
