# SaveBoard 숏폼 6편 — 2026-09-21

- 담당 패스: shortform-writer (지정된 `.claude/agents/shortform-writer.md` 없음 —
  `marketing/hooks/2026-09-21.md` 참고 후 코드로 직접 검증)
- ⚠️ **이번 배치도 "훅 위에 쓴" 게 아닙니다.** 1단계(훅 리서치)가 이번 사이클도
  검증 가능한 인용을 하나도 못 건졌습니다 — 2026-07-19부터 매주 반복된 동일한
  `WebFetch` 차단 (사유는 hooks 파일 참고, 이번엔 블로그/뉴스레터 도메인까지
  전부 막힌다는 것도 재확인했습니다). 그래서 아래 6편은 (a) 실제 앱 동작 —
  코드로 직접 확인함, (b) 페르소나에 대한 일반적 상황 묘사에 기대어 썼습니다.
  **정체불명의 발화를 누군가의 인용처럼 꾸미지 않았습니다.**
- 화면 지시는 전부 서브에이전트가 코드를 직접 읽고 검증한 내용(`App.tsx`,
  `CategoryFilter.tsx`, `SharedBoardPage.tsx`, `ProfileMenu.tsx`) — 추측으로 쓴
  화면 동작 없음. 각 편 하단 "근거"에 파일·줄번호 표기.
- 09-14 배치에서 이미 쓴 소재(보드 공유 미리보기 이미지, iOS 공유 익스텐션,
  안드로이드 send-intent, 태그 칩)는 이번 배치에서 반복하지 않음 — 4개의 새
  화면 동작을 찾아서 씀.
- 공통 고지: 앱 로고/제품명 첫 3초 노출 없음. 실제로 존재하지 않는 토스트·숫자
  표시는 화면 지시에 넣지 않았음(아래 A-2 참고 — 코드에 성공 메시지가 있지만
  실제로는 화면에 보이지 않는다는 걸 확인하고 그렇게 적었음).

---

## A. 화면 데모 4편

### A-1. [KO] 보드 순서, 손가락으로 바로 바꾼다

```
형식: 릴스/숏츠 (한국어)
길이: 약 13초
소재: 보드(카테고리) 칩 드래그 순서 변경 — 검증된 코드
종착점: 정리(오거나이즈)
```

[0-3초] 화면: 텍스트 오버레이만, 앱 화면 없음.
　　자막: "자주 보는 보드를 맨 앞으로"
　　음성: "이거 순서 좀 바꾸고 싶은데."

[3-10초] 화면: 폰 화면 녹화. 헤더 아래 보드 칩 목록에서 뒤쪽에 있는 칩을
　　**꾹 눌러서(약 0.2초)** 살짝 떠오르듯 반투명해지는 순간부터 시작 → 원하는
　　위치까지 옆으로 드래그 → 드롭할 자리에 보라색 세로선(드롭 인디케이터)이
　　뜨는 것을 보여주고 → 손을 뗀다 → 즉시 순서가 바뀐 채로 칩이 재배열.
　　자막: "꾹 눌러서, 옮기고, 놓으면 끝"
　　음성: "누르고 있다가 그대로 옮기면 돼. 저장 버튼 따로 없어."

[10-13초] 화면: 바뀐 순서 그대로의 칩 목록 클로즈업(고정).
　　자막: "다음에 열어도 이 순서 그대로"
　　CTA: "앱스토어/플레이스토어에서 SaveBoard 검색"

**촬영 시 주의**
- 커스텀 보드가 최소 2개 이상 있어야 드래그가 의미 있게 보임(3개 이상 권장).
- "전체" 칩은 드래그 대상도, 드롭 위치도 될 수 없음 — 실수로 그 칩을 옮기려는
  장면이 찍히지 않게 주의.
- 화면에 별도 "저장됨" 토스트나 알림이 뜨지 않음 — 조용히 순서만 바뀜(뒤에서
  서버에 저장되는 것뿐). 없는 토스트를 편집으로 만들어 넣지 말 것.

**근거**: `src/app/App.tsx:949-963`(`handleReorderCategory`),
`src/app/components/CategoryFilter.tsx:36-92`(드래그 구현, `react-dnd`),
`src/app/App.tsx:39,49`(`TouchBackend`, `delayTouchStart: 200`).

---

### A-2. [EN] Paste a link, watch it fill itself in

```
형식: Reel/Short (English)
길이: ~14s
소재: Add-link flow — URL paste → auto title/thumbnail fetch, verified code
종착점: 저장 (기록)
```

[0-3s] 화면: no app UI. Hand holding phone, a link visible in another app
　　(browser address bar or a messaging app).
　　자막: "You don't have to type anything but the link."
　　음성: "Just the link. That's it."

[3-11s] 화면: Screen recording. Tap the center **+** button in the bottom
　　nav → "Add to SaveBoard" modal opens, input already focused → paste the
　　URL → tap **Add** → button shows a brief spinner ("Adding…") → modal
　　closes.
　　자막: "Paste. Tap Add. Done."
　　음성: "Paste it in, hit add."

[11-14s] 화면: cut to the board grid — the new card is now sitting there
　　with a real fetched thumbnail and title already filled in (no manual
　　typing of either).
　　자막: "Title, image — it already knows"
　　CTA: "Search SaveBoard"

⚠️ **Do not show a "Link added!" success toast inside the modal** — that
message exists in the code but the modal closes in the same instant it would
appear, so it's never actually visible on screen. Cut straight from the
spinner to the modal closing to the new card. If the account you're
recording with is already at its Free-plan save limit, tapping Add opens an
upgrade screen instead of adding — check the count before recording, or use
a account with room.

**근거**: `src/app/App.tsx:736-795`(`handleAddUrl`),
`src/app/App.tsx:1533-1622`(modal UI),
`src/app/App.tsx:1572`(success message that never renders — see caveat),
`src/app/App.tsx:739`(free-plan cap redirects to upgrade instead of adding).

---

### A-3. [KO] 받은 사람이 누르는 건 버튼 하나 — "내 보드에 저장"

```
형식: 릴스/숏츠 (한국어)
길이: 약 15초
소재: 공유받은 보드에서 "Save to My Board" 원탭 가져오기 — 검증된 코드
종착점: 공유받은 링크를 실제로 자기 것으로 만드는 순간 (09-14의 "미리보기 이미지"
      편과는 다른, 받는 사람 쪽 액션임)
```

[0-3초] 화면: 텍스트 오버레이만.
　　자막: "친구가 보내준 보드, 구경만 하다 끝나지 않게"
　　음성: "이거 나도 갖고 싶은데."

[3-11초] 화면: (둘째 기기/계정) 공유 링크로 들어간 화면 — 보드 그리드,
　　"Shared by [이름]" 표시. 화면 아래 **"Save to My Board"** 버튼 탭 →
　　버튼이 "Saving…" 스피너로 바뀜 → 잠시 후 버튼이 초록색 **"Go to my
　　board"**로 바뀌고, 그 위에 결과 요약("N개 추가됨" 형태의 텍스트)이 뜸.
　　자막: "버튼 하나로 내 계정에 복사"
　　음성: "이 버튼 한 번만 누르면 저 보드가 내 보드가 돼."

[11-15초] 화면: "Go to my board" 탭 → 앱으로 들어가서 방금 저장된 보드가
　　내 보드 목록에 채워져 있는 것을 보여줌.
　　자막: "내 보드에 그대로"
　　CTA: "플레이스토어/앱스토어에서 SaveBoard"

**촬영 시 주의**
- 반드시 **다른 계정**(받는 사람 역할)으로 로그인한 상태에서 눌러야 함 —
  본인 보드를 자기 계정에 또 저장하는 장면이 되지 않게.
- 이미 그 보드의 링크를 일부 갖고 있으면 "이미 있어서 건너뜀" 문구가 같이
  뜰 수 있음 — 깔끔한 화면을 원하면 링크를 하나도 안 가진 새 계정으로 촬영.
- 카카오톡 인앱 브라우저로 열면 이 버튼 대신 "앱에서 열기" 안내가 뜸 —
  이 편은 일반 브라우저(또는 앱 딥링크)로 연 화면이어야 함.
- 09-14에 이미 쓴 "미리보기 이미지가 보드마다 다르게 뜬다"는 편과는 완전히
  다른 동작(그건 보내는 쪽 미리보기, 이건 받는 쪽 가져오기)이니 혼동해서
  같은 편으로 편집하지 말 것.

**근거**: `src/app/components/SharedBoardPage.tsx:261-271`("Save to My
Board" 버튼), `:97-181`(`handleSaveBoard` — 보드 생성/중복 제거/무료 한도
체크/결과 요약), `:174`(1.2초 후 자동 이동 로직).

---

### A-4. [EN] Watch the bar before it turns red

```
형식: Reel/Short (English)
길이: ~12s
소재: Free-plan usage bars in the profile menu — verified code
종착점: 업그레이드 (Pro 전환 유도)
```

[0-3s] 화면: no app UI. Text overlay only.
　　자막: "You don't find out you're full until you are."
　　음성: none or light VO.

[3-9s] 화면: Screen recording. Tap the avatar (top right) → dropdown opens
　　showing three progress bars: **Saves**, **Boards**, **Storage**, each
　　with a "used / limit" number. At least one bar should be near its cap
　　(orange) for this to read clearly on screen.
　　자막: "See exactly what's left"
　　음성: "It just shows you — right there."

[9-12s] 화면: Tap **Upgrade to Pro** below the bars → the upgrade/pricing
　　screen opens.
　　자막: "Or just... don't hit the wall"
　　CTA: "Search SaveBoard"

⚠️ Must record with a **Free-plan account**, not Pro — Pro accounts show ∞ /
higher caps and the bars lose their point. A brand-new, nearly-empty account
also won't read well on camera; use (or seed) an account that's genuinely
close to one of the three limits so the orange/red state is visible, not a
flat green bar.

**근거**: `src/app/components/ProfileMenu.tsx:100-125`(사용량 바 3종:
Saves/Boards/Storage, 80%↑ 주황, 100%↑ 빨강).

---

## B. 텍스트형 (릴레이터블) 2편 — 인용 아님, 명시

두 편 다 **특정인의 발화를 인용한 게 아닙니다.** 훅 리서치가 이번에도 검증
가능한 인용을 못 건졌기 때문에, 누구나 겪는 일반적 상황을 원문 카피로 재구성한
것입니다. 어느 커뮤니티/게시물에서 그대로 가져온 문장이 아니며, 인용부호로
특정 발화처럼 보이게 편집하지 말 것.

### B-1. [KO] 저장은 하는데, 다시 보진 않는다

```
형식: 텍스트형 카드/릴스 (한국어) — 화면 녹화 없음, 정적/슬로우 텍스트
길이: 약 9초
근거: 일반적으로 널리 알려진 행동 패턴 묘사 — 특정 게시물·인물 인용 아님.
      (참고: 이번 검색에서 비슷한 취지의 문장을 "북마크 방치"라는
      주제로 여러 요약에서 접했지만, 원문을 열어 대조하지 못해 인용으로
      쓰지 않았습니다 — hooks 파일의 "근접 미스" 항목 참고.)
```

[0-5초] 자막(한 줄씩): "저장 버튼은 열심히 눌렀다" → "그다음엔 안 열어봤다"
　　음성: 담백한 내레이션 또는 없음.

[5-9초] 화면: 실제 앱 화면 녹화로 전환 — 보드 그리드(카드 여러 장).
　　자막: "그래도 여기 있으면, 찾을 순 있으니까"
　　CTA: "SaveBoard 검색"

---

### B-2. [EN] Somewhere between "save for later" and "later"

```
형식: Text-on-screen (English) — no screen recording, static/slow text
길이: ~10s
근거: General, widely-recognized behavior — not attributed to any specific
      post or person. Original copy, not a lifted quote; do not add
      quotation marks to make any line look like a sourced quote.
```

[0-6s] 자막(한 줄씩): "You saved it for later." → "Later never actually
　　arrives." → "It's still there, though."
　　음성: light VO or none.

[6-10s] 화면: cut to real app footage — board grid, a few cards.
　　자막: "Whenever later shows up."
　　CTA: "Search SaveBoard"

---

## 촬영 전 공통 체크

- 앱 로고·제품명은 각 편 첫 3초에 노출하지 않음(문제 먼저, 해결은 뒤).
- 화면 지시는 전부 위 "근거" 파일을 따라감 — 촬영 중 실제 동작이 다르면
  (버전 차이 등) 화면 지시보다 실제 화면을 우선하고, 이 문서에 그 차이를
  적어 다음 배치에 반영.
- A-2/A-4는 계정 상태(저장 한도 근접 여부, Free/Pro)에 따라 화면이 달라지므로
  촬영 전 위 "촬영 시 주의"를 반드시 확인.
- B-1/B-2는 인용이 아니므로 인용부호로 특정 발화처럼 보이게 편집하지 말 것 —
  훅 리서치가 검증하지 못한 것을 검증된 것처럼 보이게 하는 셈이 됨.
