# SaaSHub submission — PeriodVol

⚠️ **This file lives in the SaveBoard repo, not PeriodVol's own `marketing/`.** This routine's
instructions are to write everything into the SaveBoard repo's `marketing/`; PeriodVol's own
repo (`michcopski8036/periodvol`) was cloned read-only this run to check for duplication, but
this session has no push access there. Once acted on, copy the relevant status line into
PeriodVol's own `marketing/offsite-listings.md` checklist so that file stays authoritative for
PeriodVol. See `marketing/directory-targets-2026-09-03.md` for the full rationale and the
network-access caveat that applies to every claim below.

Destination: https://www.saashub.com/submit. Free, self-serve. SaaSHub already carries
period-tracker compare pages (e.g. `saashub.com/compare-period-tracker-vs-clue`), confirmed via
WebSearch, so the category exists; PeriodVol does not currently appear in it.

⚠️ Field names below are inferred from third-party "how to submit" guides — this session could
not open SaaSHub's own submission form. **Open it directly before pasting.**

## Fields to paste

- **Product name:** PeriodVol
- **Website URL:** https://periodvol.app — no `www` (per PeriodVol's own
  `marketing/offsite-listings.md`: the `www` host does not resolve)
- **Tagline:**
  > Know how much you bleed, not just when
- **Categories:** Health & Fitness / Period Tracker / Women's Health (exact SaaSHub taxonomy
  unverified this run — pick the closest match on the live form)
- **Rivals / competitors field:** Clue, Flo, drip, Euki, Periodical
- **Pricing type:** Freemium — one-time in-app purchase, not a subscription. Write the price in
  **US$**, SaaSHub's audience default per PeriodVol's own currency convention:
  PeriodVol Pro US$2.99 (one-time)
- **Description** (canonical short description, from PeriodVol's own
  `marketing/offsite-listings.md`):
  > PeriodVol is a period tracker that measures what most apps ignore: how much you actually
  > bleed. Log the pads, tampons or cups you use and it estimates your flow in millilitres —
  > daily and per cycle — against the 80 ml clinical threshold for heavy menstrual bleeding.
  > So when a doctor asks "are your periods heavy?", you have a number. Everything stays on
  > your phone. Not a medical device.
- **Platforms:** iOS, Android, Web
- **Logo / screenshots:** same assets already queued for PeriodVol's own Product Hunt launch
  (logo + logging, cycle total vs the 80 ml line, doctor report) — that launch is recorded as
  blocked on exactly these screenshots in PeriodVol's `offsite-listings.md`; producing them
  once would unblock both.
- **Verification email:** PeriodVol's own domain is receive-only (Cloudflare Email Routing, no
  SMTP — see PeriodVol's `listicle-outreach-drafts.md` for why outreach sends from
  `creatorsloftperth@gmail.com` instead), so a domain-verified SaaSHub listing is likely not
  available without a mail-host change. Submit unverified rather than using a personal address
  that won't match `periodvol.app`.

## Hard rules carried over from PeriodVol's own file (do not drop when pasting)

- **No medical claims.** The description above already avoids "diagnose/detect/screen" — keep
  it exactly as written rather than shortening it into something that reads as a claim.
- If the SaaSHub form asks for a submitter name/role, disclose **Mihee Youn, founder** — never
  submit as an anonymous user.

## Status

📝 Drafted 2026-09-03, not submitted.
