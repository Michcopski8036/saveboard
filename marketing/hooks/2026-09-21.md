# Hooks — 2026-09-21

**0 verified hooks this week.** Quotes only, never usernames or handles — nobody
here is identified. This is now the **same infrastructure wall reported in every
prior batch** (07-19, 07-20, 07-27, 08-10, 08-17, 08-24, 08-31, 09-14 all record
identical `EGRESS_BLOCKED` findings) — going on **two months** unfixed. This file
exists to keep confirming it's still broken, not because the search effort was
thin.

## What I tried this week

Korean and English run separately, as the brief asks:

- English: `mymind app review "too expensive" OR "confusing" reddit bookmark`,
  `coach OR volunteer coordinator "keep track of links" group chat frustrating`,
  `Notion bookmark database "too much work" OR "gave up" reddit`, `"group chat"
  lost the link "can't find it" bookmark reddit`, `Raindrop.io review "too
  complicated" OR "gave up" site:reddit.com`.
- Korean: `"단톡방" 링크 잃어버림 찾기 힘들다`, `"학부모 단톡방" 링크 정리 앱 추천`.

Korean was thin again — results were almost entirely unrelated how-to content
(rejoining a KakaoTalk group, school-communication apps), nothing matching the
persona this brief asks for (a parent/organiser losing a link inside an existing
chat, not looking for a new chat app).

## The wall, confirmed again and widened

Tested `WebFetch` against domains from **outside** the review-site list this time,
specifically to check whether the block is per-domain or blanket. It is blanket:

- `reddit.com`, `news.ycombinator.com`, `apps.apple.com` — the usual suspects,
  `EGRESS_BLOCKED` as always.
- `maily.so`, `ryanleach.substack.com`, `dev.to` — ordinary blog/newsletter
  domains, no special sensitivity, also `EGRESS_BLOCKED`.

That last group matters: this isn't a policy specifically excluding
review/forum sites, it's every external domain this session tried. `WebSearch`
still only paraphrases (confirmed again — see the two near-misses below), so
there is currently no path in this session from "a promising search snippet" to
"a verified verbatim quote with a working source link."

## Near-misses `WebSearch` surfaced but I could not verify

Not hooks — cannot confirm exact wording or that these lines mean what the
AI-paraphrase implies, since I could not open either source page:

1. A search summary attributed to `ryanleach.substack.com/p/the-last-app-youll-ever-need-until`
   described the author calling Notion "completely useless as a digital archive"
   after trying to save interesting things they found online, which then "collect
   dust." If a human opens this post and the wording checks out, it's a strong
   bookmark-clutter hook — persona would be the digital-packrat / info-hoarder,
   not specifically group chats.
2. A search summary attributed to `dev.to/itzami/how-i-use-notion-as-a-developer-36lc`
   described the author having "way too many development blog posts saved" with
   "no way I would ever find [them] here." Same caveat — paraphrase, not a
   confirmed quote, and this persona (developer bookmarking blog posts) is a
   weaker fit for SaveBoard's target audience than the group-chat personas.

Both would take one human page-load each to confirm or kill.

## Recommendation, unchanged from last several batches

This needs a person with a browser, or a `WebFetch` allowlist change, not another
Monday of the same `WebSearch` queries. Until one of those happens this pass will
keep landing at zero.
